name: Sidebar rerendering workflow

on:
  workflow_dispatch:

jobs:
  singleJob:
    runs-on: ubuntu-latest
    steps:
    - name: Rerender sidebar
      run: |
          cd ./docs
          python rerenderSidebar.py
    - name: Push changes
      uses: ad-m/github-push-action@master
      with:
        github_token: ${{ secrets.GITHUB_TOKEN }}
        branch: ${{ github.ref }}
